<div class="new_notice">
  <%= form_for @notice do |f| %>
    <p>
      <%= f.label :heading %>
      <%= f.text_field :heading, :autofocus => true %>
    </p>

    <p>
      <%= f.label :message %>
      <%= f.text_area :message %>
    </p>

    <p>
      <%= f.label :category_id %>
      <%= f.select :category_id, Category.all.map {|category|[category.name, category.id]}, :include_blank => true %>
    </p>

    <p>
      <%= f.label :Image %>
      <%= f.file_field :file %>
    </p>

    <p>
      <%= f.label :date_from %>
      <%= f.date_field(:date_from, value: Date.today) %>
    </p>

    <p>
      <%= f.label :date_to %>
      <%= f.date_field(:date_to, value: '2015-12-31') %>
    </p>

  <%= f.submit %>
<% end %>
</div>
