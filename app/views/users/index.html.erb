<h2>All Users</h2>
  <ol>
    <% @users.each do |user| %>
      <li>
        <%= user.name %>
        <%= button_to('Delete User', user, :method => 'delete', :data => {:confirm => 'You sure homie???'}) %>
      </li>
    <% end %>
  </ol>